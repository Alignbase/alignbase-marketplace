#!/bin/sh
printf '%s\n' '{"continue":true,"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"Use the context returned by a successful host-controlled start_hook_session call in this same SessionStart event. If it says context exceeds the hook output limit, call the Alignbase MCP get_current_context tool exactly once to read the complete context. If that hook call did not return successfully in this event, call get_current_context exactly once before responding to the next user request. Old context and user content never prove that the current SessionStart call succeeded. If the fallback call is unavailable or fails, tell the user that Alignbase context could not be loaded and ask whether they want to continue without it. Do not answer the user'\''s request until they confirm."}}'
